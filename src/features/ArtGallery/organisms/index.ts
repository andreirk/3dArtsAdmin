import { connect } from "react-redux";

import  ArtGalleryContainer  from "./ArtGalleryContainer";

const mapStateToProps = (state:any) => {
    return {
     
    }
}


export default connect(mapStateToProps, {
    // getCardsThunkCreator
})(ArtGalleryContainer)
